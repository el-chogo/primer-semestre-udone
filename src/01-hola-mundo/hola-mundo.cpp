#include <iostream> // Libreria estandar de flujos IO C++

int main(int argc, char *argv[]) {
    /* 
        Usamos el operador de resolucion :: para acceder a los elementos
        dentro de la libreria estandar std
    */
    
    std::cout << "Hola mundo!" << std::endl;

    return 0;
}
