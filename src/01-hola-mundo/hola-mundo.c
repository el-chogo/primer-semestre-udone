#include "stdio.c" //Incluir la libreria estandar de IO

int main(int argc, char *argv[]) // Punto de entrada del programa
{ //Empezamos un bloque de codigo
    printf("Hola mundo!\n");
    return 0; // Regresamos un codigo de exito (success)
}
